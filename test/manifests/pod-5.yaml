---
apiVersion: v1
kind: Pod
metadata:
  name: test-pod-5
  namespace: pvc-autoscaler-system
spec:
  volumes:
    - name: app-vol
      persistentVolumeClaim:
        claimName: test-pvc-5
  containers:
    - name: test-pod-5
      image: alpine:3.23.3
      command:
        - sleep
        - infinity
      volumeMounts:
        - mountPath: /app
          name: app-vol
