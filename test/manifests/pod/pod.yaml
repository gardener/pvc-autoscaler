---
apiVersion: v1
kind: Pod
metadata:
  name: test-pod
spec:
  volumes:
    - name: app-vol
      persistentVolumeClaim:
        claimName: test-pvc-fs
  containers:
    - name: test-pod
      image: debian:latest
      command:
        - sleep
        - infinity
      volumeMounts:
        - mountPath: /app
          name: app-vol
