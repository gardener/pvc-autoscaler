---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-pvc-1
spec:
  storageClassName: openebs-lvm
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
