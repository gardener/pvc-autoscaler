---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-pvc-block
spec:
  storageClassName: openebs-lvm
  volumeMode: Block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
