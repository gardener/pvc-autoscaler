---
apiVersion: v1
kind: Pod
metadata:
  name: test-pod-2
  namespace: pvc-autoscaler-system
spec:
  volumes:
    - name: app-vol
      persistentVolumeClaim:
        claimName: test-pvc-2
  containers:
    - name: test-pod-2
      image: alpine:3.23.3
      command:
        - sleep
        - infinity
      volumeMounts:
        - mountPath: /app
          name: app-vol
